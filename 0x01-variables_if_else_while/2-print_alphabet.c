#include <stdio.h>

/**
 * main - Print a - z in small letters
 *
 * Return: Always (Success)
 */

int main(void)
{
	char alph;

	for (c = 'a'; c <= 'z'; c++)
		putchar(alph);

	putchar('\n');
	return (0);
}
