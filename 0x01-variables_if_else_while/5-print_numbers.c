#include <stdio.h>

/**
 * main - Prin numbers from 0 to 10
 *
 * Returns: Always (Success)
 *
 */

int main(void)
{
	int numb;

	for (numb = 0; numb <= 9; numb++)
	{
		printf("%d\n", numb);
	}

	return (0);
}
